% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ExpandAgeSeason.R
\name{ExpandAgeSeason}
\alias{ExpandAgeSeason}
\title{Expand an annual-age-by-season schedule onto quarterly age classes}
\usage{
ExpandAgeSeason(x, AgeClasses)
}
\arguments{
\item{x}{A matrix with dimensions \verb{[annual_age, season]}, or an array with
additional trailing dimensions (e.g. \verb{[annual_age, season, stock]}),
indexed from annual age 0 in row 1.}

\item{AgeClasses}{Numeric vector of quarterly age classes to expand onto,
e.g. \code{Classes(stock)}.}
}
\value{
An array with the first dimension replaced by \code{length(AgeClasses)},
keeping the season dimension and any trailing dimensions of \code{x}.
}
\description{
The conditioning model tabulates life-history schedules (weight,
maturity, etc.) by annual age (rows, starting at annual age 0) and season
(columns). An MSEtool \code{Stock} with a quarterly age structure instead
indexes age by quarterly age classes (\verb{0, 0.25, 0.5, ...}). This expands
the annual-age rows onto the quarterly age classes, holding the value
constant across the sub-year age classes within each annual age, while
keeping the season dimension intact.
}
